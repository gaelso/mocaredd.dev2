% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_make_formula_U.R
\name{fct_make_formula_U}
\alias{fct_make_formula_U}
\title{Make the carbon stock uncertainty formula of a land use (IPCC Approach 1)}
\usage{
fct_make_formula_U(.c_el, .c_unit, .version = 1)
}
\arguments{
\item{.c_el}{Vector of carbon elements, among "AGB", "BGB", "RS", "DW", "LI",
"SOC", "ALL". Other elements (e.g. "DG_ratio") are ignored.}

\item{.c_unit}{Carbon unit. \code{.version = 1}: a single value ("DM" or "C")
for the land use; CF applies to biomass (AGB, BGB) only.
\code{.version = 2}: a vector aligned with \code{.c_el}, one unit per
element; CF applies to each element in DM. RS takes the unit of AGB.}

\item{.version}{Template version, 1 (default) or 2.}
}
\value{
A character value with the standard error formula of the carbon stock.
}
\description{
Companion of \code{fct_make_formula2()}: same inputs, returns the
formula of the standard error of the carbon stock, built from the
terms of \code{fct_make_formula2()} with the IPCC Approach 1 rules
(IPCC 2006, Vol. 1, Ch. 3, Eq. 3.1 and 3.2):
- product: \code{x * y * sqrt((x_se / x)^2 + (y_se / y)^2)},
- sum: \code{sqrt(x_se^2 + y_se^2)}.

\if{html}{\out{<div class="sourceCode">}}\preformatted{         Composite factors are resolved first:
         - \code{(1 + RS)}: standard error \code{RS_se},
         - \code{(AGB + BGB)}: standard error \code{sqrt(AGB_se^2 + BGB_se^2)}.

         Evaluate it with each element and its standard error as
         \code{<element>} and \code{<element>_se} (e.g. \code{AGB}, \code{AGB_se},
         \code{CF}, \code{CF_se}). Terms are assumed independent. A factor
         equal to 0 in a product returns NaN.
}\if{html}{\out{</div>}}
}
\examples{
fct_make_formula_U(.c_el = c("AGB", "RS"), .c_unit = "DM")
#> "AGB * (1 + RS) * CF * sqrt((AGB_se / AGB)^2 + (RS_se / (1 + RS))^2 + (CF_se / CF)^2)"

fct_make_formula_U(.c_el = c("AGB", "RS", "DW"), .c_unit = c("DM", NA, "C"), .version = 2)

## Evaluate
env <- list(AGB = 250, AGB_se = 25, RS = 0.24, RS_se = 0.05, CF = 0.47, CF_se = 0.02)
eval(parse(text = fct_make_formula_U(c("AGB", "RS"), "DM")), env)

}
